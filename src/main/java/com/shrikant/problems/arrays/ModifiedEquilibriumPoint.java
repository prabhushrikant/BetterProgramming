package com.shrikant.problems.arrays;

//This will be an interesting question to ask in case of candidate interview (lower level)
//https://practice.geeksforgeeks.org/problems/unsorted-array4925/1

// Given an unsorted array of size N. Find the first element in array such that all of its left elements are smaller and all right elements to it are greater than it.
// Note: Left and right side elements can be equal to required element. And extreme elements cannot be required element.
public class ModifiedEquilibriumPoint {
    
}
